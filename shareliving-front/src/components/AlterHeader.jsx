const AlterHeader = () => {
    return (
        <header id="altHeader"> { /** 상하단으로 네비게이션 분리되는 두 번째 안 */ }
            <div>상단 요소 - 유저 컨트롤, 종합검색 등</div>
            <nav>로고, 네비게이션</nav>
        </header>
    );
};

export default AlterHeader;